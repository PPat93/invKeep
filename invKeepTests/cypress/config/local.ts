const config = {
    environment: `local`,
    baseUrl: `http://localhost:4200`,
    viewportWidth: 1200,
    viewportHeight: 1200,
    env: {
        backendUrl: `http://localhost:3000/api/assets`
    }
}

export default config;